version: '2'

services:
  app:
    container_name: 'studioresourcemanagermt-app'
    image: 'studioresourcemanagermt-app'
    build:
      context: .
      dockerfile: DockerFileReact
	  args
	  - REACT_APP_API_URL=http://192.168.99.100:81/
    environment:
       - REACT_APP_API_URL=http://192.168.99.100:81/
    volumes:
      - .:/var/www/app
    ports:
     - "80:80"
    networks:
      - studioresourcemanagermt-network

  api:
    container_name: 'studioresourcemanagermt-api'
    image: 'studioresourcemanagermt-api'
    build:
      context: .
      dockerfile: DockerFile
    environment:
       - ASPNETCORE_URLS=http://+:81
       - ConnectionStrings__ApplicationDbContext=Host=studioresourcemanagermt-db;Persist Security Info=False;User ID=postgres;Password=test;Database=studioresourcemanagermt;
       - DatabaseProvider=POSTGRESQL
       - MigrateDatabase=true
    volumes:
      - .:/var/www/api
    ports:
     - "81:81"
    depends_on:
     - "db"
    networks:
      - studioresourcemanagermt-network

  db:
    container_name: 'studioresourcemanagermt-db'
    image: postgres:9.6-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: test
      POSTGRES_DB: studioresourcemanagermt
    networks:
      - studioresourcemanagermt-network
    ports:
     - "5433:5432"
    restart: always

  watchtower:
    container_name: 'watchtower'
    image: v2tec/watchtower
    restart: on-failure
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    command: --interval 30
    networks:
      - studioresourcemanagermt-network
volumes:
     pgdata:

networks:
    studioresourcemanagermt-network:
       driver: bridge